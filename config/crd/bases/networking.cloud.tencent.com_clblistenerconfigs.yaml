---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: clblistenerconfigs.networking.cloud.tencent.com
spec:
  group: networking.cloud.tencent.com
  names:
    kind: CLBListenerConfig
    listKind: CLBListenerConfigList
    plural: clblistenerconfigs
    singular: clblistenerconfig
  scope: Cluster
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: CLBListenerConfig is the Schema for the clblistenerconfigs API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: CLBListenerConfigSpec defines the desired state of CLBListenerConfig
              properties:
                foo:
                  description:
                    Foo is an example field of CLBListenerConfig. Edit clblistenerconfig_types.go
                    to remove/update
                  type: string
              type: object
            status:
              description: CLBListenerConfigStatus defines the observed state of CLBListenerConfig
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
