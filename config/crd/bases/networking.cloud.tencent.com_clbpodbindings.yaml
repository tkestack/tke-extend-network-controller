---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: clbpodbindings.networking.cloud.tencent.com
spec:
  group: networking.cloud.tencent.com
  names:
    kind: CLBPodBinding
    listKind: CLBPodBindingList
    plural: clbpodbindings
    singular: clbpodbinding
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CLBPodBinding is the Schema for the clbpodbindings API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CLBPodBindingSpec defines the desired state of CLBPodBinding
            properties:
              lbId:
                example: lb-xxx
                type: string
              lbPort:
                format: int64
                type: integer
              lbRegion:
                type: string
              podName:
                type: string
              protocol:
                type: string
              targetPort:
                format: int64
                type: integer
            required:
            - lbId
            - lbPort
            - podName
            - protocol
            - targetPort
            type: object
          status:
            description: CLBPodBindingStatus defines the observed state of CLBPodBinding
            properties:
              state:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
            required:
            - state
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
