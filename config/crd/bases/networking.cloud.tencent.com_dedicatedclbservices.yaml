---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: dedicatedclbservices.networking.cloud.tencent.com
spec:
  group: networking.cloud.tencent.com
  names:
    kind: DedicatedCLBService
    listKind: DedicatedCLBServiceList
    plural: dedicatedclbservices
    singular: dedicatedclbservice
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DedicatedCLBService is the Schema for the dedicatedclbservices
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DedicatedCLBServiceSpec defines the desired state of DedicatedCLBService
            properties:
              existedLbIds:
                items:
                  type: string
                type: array
              lbAutoCreate:
                properties:
                  configJson:
                    type: string
                  enable:
                    type: boolean
                type: object
              lbRegion:
                type: string
              listenerConfig:
                type: string
              maxPort:
                format: int64
                type: integer
              minPort:
                format: int64
                type: integer
              ports:
                items:
                  properties:
                    addressPodAnnotation:
                      type: string
                    protocol:
                      type: string
                    targetPort:
                      format: int64
                      type: integer
                  required:
                  - protocol
                  - targetPort
                  type: object
                type: array
              selector:
                additionalProperties:
                  type: string
                type: object
              vpcId:
                type: string
            required:
            - maxPort
            - minPort
            - ports
            - selector
            type: object
          status:
            description: DedicatedCLBServiceStatus defines the observed state of DedicatedCLBService
            properties:
              lbList:
                items:
                  properties:
                    autoCreate:
                      type: boolean
                    lbId:
                      type: string
                    maxPort:
                      format: int64
                      type: integer
                  required:
                  - autoCreate
                  - lbId
                  type: object
                type: array
            required:
            - lbList
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
